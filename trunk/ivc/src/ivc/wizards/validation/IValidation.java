package ivc.wizards.validation;

/**
 * 
 * @author alexm
 * 
 *         Interface used for validators
 */
public interface IValidation {
	/**
	 * Returns true if the validation turned out ok else returns false
	 * 
	 * @return true if the validation turned out ok else returns false
	 */
	public boolean isValid();
}
